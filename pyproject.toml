[project]
name = "hands_on_neuroai"
version = "0.1.0"
description = "Hands on NeuroAI"
requires-python = ">=3.11"

[tool.ruff]
line-length = 88
target-version = "py311"
select = ["E", "F", "B", "I"]
ignore = []

[tool.pytest.ini_options]
testpaths = ["tests"]
